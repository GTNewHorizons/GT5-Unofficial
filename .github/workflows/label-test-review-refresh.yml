name: Label Test Review Refresh

# Generated by GTNewHorizons/Label-Sync-GTNH. Do not edit by hand: this file is overwritten on
# every distribution run.

on:
  pull_request_review:
    types:
      - submitted
      - edited
      - dismissed

permissions:
  contents: read

jobs:
  refresh-label-test:
    name: Refresh Label Test
    permissions:
      actions: write
      contents: read
      checks: write
      pull-requests: read
    uses: GTNewHorizons/Label-Sync-GTNH/.github/workflows/refresh-label-test.yml@master
    with:
      label_sync_repository: GTNewHorizons/Label-Sync-GTNH
      label_sync_ref: master
    secrets: inherit
