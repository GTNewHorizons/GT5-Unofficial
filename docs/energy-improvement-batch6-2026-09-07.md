# Energy improvements batch 6: native AE2 simulation reuse

This completes the bounded accepted-path optimization portion of consolidated-plan item 9. AE2 commit ebd2a9ee7 on codex/ae2-energy-baseline follows measurement baseline 77b88f3de. The detailed implementation, compatibility results and remaining benchmarks are recorded in that repository's docs/energy-simulation-reuse-2026-09-07.md. GT production code is unchanged.

EnergyGridCache now reuses equivalent native descendant simulations within one quartz-fiber simulation/commit operation. Exact native classes, offered amount and complete visited-set equivalence guard reuse. Storage commits, external callbacks and extra writes invalidate quotes; finally cleanup handles exceptions and nested injection. Visited snapshots are bounded at 128 grids, with original traversal beyond the limit. Public energy interfaces, GTPowerSink whole-amp accounting, P2P, routing and separate correctness issues are unchanged.

In the fixed backlink-first depth-32 native fixture, provider simulations during commit fell from 1,055 to 125, with 63 provider commits and exactly 256 AE stored unchanged. Native leaf simulations inside commit fell from 32 to one. Depths 0, 1, 2, 4, 8 and 16 also pass; unknown-provider/storage baseline counts remain unchanged. These are verified work reductions, not CPU, allocation-byte, MSPT or TPS measurements. The new bounded snapshots have a memory cost that needs profiling.

Baseline checks passed before editing, including four new native behavior cases before production changes. All 86 AE2 tests pass after implementation, with no failures/errors/skips; formatting and both Checkstyle tasks pass. Nine new regression cases cover native threshold events, synchronous simulation and committed re-entry, downstream mutation, membership changes, exception cleanup, whole GT amps, partial offers, buffers/extra, cycles and depth-130 fallback. Final diff review and whitespace checks passed.

The existing ignored child commit remainder and shared localSeen behavior remain explicitly unchanged. No live Minecraft world was available; controlled production-code fixtures cannot establish live lifecycle behavior or server gains. GT was not rebuilt for this documentation-only handoff. No clean task ran, and all eight supporting files under build/energy-improvement-2026-09-07 match their protected backup by hash.

Recommended next batch: compare baseline/candidate CPU, allocation/GC and MSPT in a representative warmed world, including shallow and deep native/mixed grids, partial and blocked offers, branching and topology churn. Keep P2P measurements separate. If no representative world is available, the next planned code batch is item 10's routing feature tests and explicit route-weight/loop-energization rules, before changing compressed topology or implementing shortest paths. Do not expand the reuse whitelist or limit without evidence. Nothing was pushed.
